import { FC } from 'react';
import { Wrapper } from './{{ cfl name }}.styled';
import { Props } from './{{ cfl name }}.types';

export const {{ cfl name }}: FC<Props> = ({}) => {
    return <Wrapper></Wrapper>;
};
