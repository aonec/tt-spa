import styled from 'styled-components';
import { Tabs } from 'antd';

export const TabsSC = styled(Tabs)`
  margin-top: 16px;
  .ant-tabs-tab-btn {
    font-weight: 500;
    font-size: 16px;
  }
`;

export const DocumentsWrapper = styled.h2`
  color: black;
`;
