import React from 'react';import {   Loader, HeaderWrap, Title, Subtitle,} from '01/_components';export const Header = (props, loading = true) => {  const {    apartmentNumber, city, street, number,  } = props;  //console.log(props);  if ((apartmentNumber && city && street && number) !== undefined) {    loading = false;  }  return (    <HeaderWrap>      <Loader show={loading} size='32'>        <Title size="32">          {`Кв. № ${apartmentNumber}`}        </Title>        <Subtitle>          {`${city}, ${street}, ${number}`}        </Subtitle>      </Loader>    </HeaderWrap>  );};