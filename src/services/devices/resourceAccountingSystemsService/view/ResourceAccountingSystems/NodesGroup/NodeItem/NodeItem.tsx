import React, { FC } from 'react';
import { Wrapper } from './NodeItem.styled';
import { NodeItemProps } from './NodeItem.types';

export const NodeItem: FC<NodeItemProps> = ({ node, segmentName }) => {
  return <Wrapper></Wrapper>;
};
