import { createDomain } from 'effector';

const domain = createDomain(
  'individualDevicesViewBySerialNumberService'
);

export const individualDevicesViewBySerialNumberService = {
  inputs: {
  
  },
  outputs: {

  },
};
