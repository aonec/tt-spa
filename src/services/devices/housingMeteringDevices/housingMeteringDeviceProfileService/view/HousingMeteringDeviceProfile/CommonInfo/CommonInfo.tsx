import React, { FC } from 'react';
import { Wrapper } from './CommonInfo.styled';
import { CommonInfoProps } from './CommonInfo.types';

export const CommonInfo: FC<CommonInfoProps> = ({}) => {
  return <Wrapper></Wrapper>
};
