import { createDomain } from 'effector';

const domain = createDomain(
  'housingMeteringDeviceProfileService'
);

export const housingMeteringDeviceProfileService = {
  inputs: {
  
  },
  outputs: {

  },
};
