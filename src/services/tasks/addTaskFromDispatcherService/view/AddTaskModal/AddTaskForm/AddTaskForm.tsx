import React, { FC } from 'react';
import { Wrapper } from './AddTaskForm.styled';
import { AddTaskFormProps } from './AddTaskForm.types';

export const AddTaskForm: FC<AddTaskFormProps> = ({}) => {
  return <Wrapper>!!!</Wrapper>
};
