import React, { FC } from 'react';
import { Wrapper } from './NodeDevices.styled';
import { NodeDevicesProps } from './NodeDevices.types';

export const NodeDevices: FC<NodeDevicesProps> = ({ node }) => {
  return <Wrapper></Wrapper>;
};
