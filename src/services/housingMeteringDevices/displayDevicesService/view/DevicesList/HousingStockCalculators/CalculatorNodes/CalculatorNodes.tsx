import React, { FC } from 'react';
import { Wrapper } from './CalculatorNodes.styled';
import { CalculatorNodesProps } from './CalculatorNodes.types';

export const CalculatorNodes: FC<CalculatorNodesProps> = ({}) => {
  return <Wrapper></Wrapper>
};
