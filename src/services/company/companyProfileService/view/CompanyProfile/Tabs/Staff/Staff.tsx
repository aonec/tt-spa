import React, { FC } from 'react';
import { Wrapper } from './Staff.styled';
import { StaffProps } from './Staff.types';

export const Staff: FC<StaffProps> = ({}) => {
  return <Wrapper></Wrapper>
};
