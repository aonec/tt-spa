import React, { FC } from 'react';
import { Wrapper } from './SwitchStage.styled';
import { SwitchStageProps } from './SwitchStage.types';

export const SwitchStage: FC<SwitchStageProps> = ({}) => {
  return <Wrapper></Wrapper>
};
