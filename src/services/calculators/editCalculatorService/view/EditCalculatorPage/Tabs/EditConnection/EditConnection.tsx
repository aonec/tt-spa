import React, { FC } from 'react';
import { Wrapper } from './EditConnection.styled';
import { EditConnectionProps } from './EditConnection.types';

export const EditConnection: FC<EditConnectionProps> = ({}) => {
  return <Wrapper></Wrapper>
};
