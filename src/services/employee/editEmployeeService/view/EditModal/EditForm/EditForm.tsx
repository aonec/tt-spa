import React, { FC } from 'react';
import { Wrapper } from './EditForm.styled';
import { EditFormProps } from './EditForm.types';

export const EditForm: FC<EditFormProps> = ({}) => {
  return <Wrapper></Wrapper>
};
