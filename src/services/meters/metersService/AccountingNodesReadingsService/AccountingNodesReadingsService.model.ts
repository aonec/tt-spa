import { createDomain } from 'effector';

const domain = createDomain(
  'AccountingNodesReadingsService'
);

export const AccountingNodesReadingsService = {
  inputs: {
  
  },
  outputs: {

  },
};
