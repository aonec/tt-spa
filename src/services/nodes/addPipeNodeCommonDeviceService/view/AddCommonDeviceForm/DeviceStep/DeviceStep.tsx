import React, { FC } from 'react';
import { Wrapper } from './DeviceStep.styled';
import { DeviceStepProps } from './DeviceStep.types';

export const DeviceStep: FC<DeviceStepProps> = ({}) => {
  return <Wrapper></Wrapper>
};
