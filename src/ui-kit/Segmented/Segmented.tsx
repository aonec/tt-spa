import React, { FC } from 'react';
import { Wrapper } from './Segmented.styled';
import { SegmentedProps } from './Segmented.types';

export const Segmented: FC<SegmentedProps> = ({}) => {
  return <Wrapper></Wrapper>
};
